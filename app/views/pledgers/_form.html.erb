<%= form_with(model: pledger) do |form| %>
  <% if pledger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pledger.errors.count, "error") %> prohibited this pledger from being saved:</h2>

      <ul>
        <% pledger.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phonenumber %>
    <%= form.text_field :phonenumber %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :expecteddate %>
    <%= form.date_select :expecteddate %>
  </div>

  <div class="field">
    <%= form.label :fullfilled %>
    <%= form.check_box :fullfilled %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
